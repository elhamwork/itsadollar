# Stripe TEST (sandbox) secret key — starts with sk_test_
# Find it at https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_...

# Origin the static site is served from (no trailing slash)
CLIENT_URL=http://localhost:8123

# Port this API listens on
PORT=4242
